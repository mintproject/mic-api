version: '3.3'

services:
  postgres:
    image: postgres
    restart: always
    environment:
      - POSTGRES_PASSWORD=test
      - POSTGRES_USER=test
      - POSTGRES_DB=mat
    volumes:
        - db_data:/var/lib/postgresql/data
    ports:
      - 5432:5432
volumes:
  db_data:
